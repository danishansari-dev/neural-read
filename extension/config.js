/**
 * Configuration variables for the NeuralRead extension.
 * This exists to centralize URLs and constants so they can be easily changed for testing/production.
 */

export const BACKEND_URL = 'http://localhost:8000';
